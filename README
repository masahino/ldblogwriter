

* lbwの使い方

$ lbw <text file>

lbwはブログを投稿するツールです。
投稿するテキストファイルを引数として指定します。

テキストの内容はHTMLのタグを付けて投稿します。

* テキストファイルの形式

先頭一行目にタイトルを指定します。
<>でカテゴリを指定できます。

例:
<カテゴリ> タイトル

本文…

* 設定ファイルの書き方

設定ファイルとして、

~/.ldblogwriter.conf

をlbw起動時に読み込みます。

